import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PMDM04_TareaEvaluativa01',
  webDir: 'www'
};

export default config;
